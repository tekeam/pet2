function downloadBlob(blob, filename){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
document.addEventListener('click',async(event)=>{const link=event.target.closest('a[href="/api/admin/export"],a[href="/api/admin/centers/export.csv"]');if(!link)return;event.preventDefault();const token=localStorage.getItem('np_token');if(!token){location.hash='/login';return}try{const response=await fetch(link.getAttribute('href'),{headers:{Authorization:`Bearer ${token}`}});if(!response.ok)throw new Error('دانلود ناموفق بود');const blob=await response.blob();downloadBlob(blob,link.getAttribute('href').includes('csv')?'ninjapet-centers.csv':'ninjapet-backup.json')}catch(error){alert(error.message)}});
window.addEventListener('hashchange',()=>{const route=location.hash.slice(1)||'/';const titles={ '/':'نینجا پت','/brands':'برندهای غذایی | نینجا پت','/foods':'پیشنهاد غذای مناسب | نینجا پت','/centers':'مراکز پت | نینجا پت','/explore':'اکسپلور | نینجا پت','/profile':'خانواده نینجا پت','/admin':'مدیریت نینجا پت'};document.title=titles[route]||'NinjaPet'});
